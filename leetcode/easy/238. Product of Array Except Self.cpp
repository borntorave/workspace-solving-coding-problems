// https://leetcode.com/problems/product-of-array-except-self/description/

#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
    vector<int> productExceptSelf(vector<int>& nums) {
        vector<int> answer = {};
        int prod = 0;

        for(int i = 0; i < nums.size(); i++)
        {
            
        }

        return answer;
    }
};

int main()
{
    Solution sol;

    vector<int> nums = {1,2,3,4};

    vector<int> ref = sol.productExceptSelf(nums);

    return 0;
}