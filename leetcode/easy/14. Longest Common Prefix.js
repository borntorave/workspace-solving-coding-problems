// https://leetcode.com/problems/longest-common-prefix/description/

/**
 * @param {string[]} strs
 * @return {string}
 */
var longestCommonPrefix = function(strs) {
    let st_sort = strs.sort();
    // ["dog", "dogger","racecar","car", "flier"]

    let ret = [];
    for(let i = 0; i < strs.length(); ++i) {
    }
};

let a1 = ["dog","racecar","car", "dogger", "flier"]
console.log(a1.sort());
